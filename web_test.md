# 任务：构建一个高保真交互式个人作品集网站

## 1. 基础架构与路由
- 采用**单页应用（SPA）**架构，使用原生JavaScript实现前端路由（Hash或History API），包含至少4个逻辑页面：首页、关于我、项目集、联系页
- 实现**浏览器前进/后退按钮支持**，路由切换时保持页面状态（如滚动位置、已展开的折叠内容）
- 所有页面切换必须带有**平滑的过渡动画**（淡出淡入或滑动效果），持续时间300-500ms，使用CSS Transition或Web Animations API

## 2. 视觉设计与主题系统
- 实现**自动深色/浅色模式切换**：根据系统偏好自动设置，同时提供手动切换按钮，切换时整个页面的颜色过渡动画持续400ms
- 设计令牌（Design Tokens）系统：定义CSS自定义属性（变量）管理颜色、字体、间距、圆角，确保全站一致性
- 字体栈：英文使用 Inter 或 Space Grotesk，中文使用系统字体回退栈（PingFang SC, Microsoft YaHei, sans-serif）
- 排版层次：至少6级标题（H1-H6）和正文、引用、代码块等样式，行高1.6-1.8，正文字号16-18px

## 3. 首页（Hero Section）
- **全视口高度（100vh）**的沉浸式首屏，背景使用**动态渐变网格**（Animated Mesh Gradient）或**交互式粒子系统**（Canvas 2D实现，粒子数量50-100个，响应鼠标移动产生排斥/吸引效果）
- 中央放置个人头像（使用占位图即可），带有**悬停3D倾斜效果**（perspective + rotateX/Y，跟随鼠标位置）
- 打字机效果展示个人标语（Typewriter Effect），速度可调，支持光标闪烁
- 底部放置**滚动指示器**（弹跳动画），点击后平滑滚动到下一章节

## 4. 关于我页面
- **时间轴布局**展示个人经历（教育、工作），时间轴必须响应式：桌面端垂直居中线条，左右交替排列；移动端垂直左侧线条，右侧排列
- 时间轴节点带有**滚动触发动画**（Scroll-triggered Animation）：当滚动到视口20%位置时，节点从透明+偏移状态渐显并归位，使用 Intersection Observer API
- 包含**技能可视化图表**：使用SVG或Canvas绘制雷达图或条形图，展示技能熟练度，图表需要有入场动画（逐条绘制或逐渐填充）
- 个人照片区域实现**视差滚动效果**（Parallax Scrolling）

## 5. 项目集页面
- **瀑布流/网格布局**（Masonry Layout）展示至少6个项目卡片，桌面端3列，平板2列，手机1列
- 每个项目卡片包含：封面图（占位图）、项目名称、技术标签、简短描述、外部链接按钮
- 实现**分类筛选功能**：顶部有筛选标签（如"前端"、"后端"、"设计"），点击后已选项目保持原位并重新排列，未选项目以缩小+淡出方式消失，使用FLIP动画技术（First, Last, Invert, Play）
- 卡片悬停效果：图片放大1.05倍，阴影加深，信息层从底部滑入显示详细技术栈
- 点击卡片打开**模态框（Modal）**：展示项目详情、大图轮播、技术挑战说明、GitHub/在线演示链接。模态框打开/关闭有缩放+淡入动画，背景添加模糊效果（backdrop-filter: blur），点击背景或按ESC键关闭

## 6. 联系页
- 包含**功能完整的联系表单**：姓名、邮箱、主题、消息内容四个字段，带实时验证（邮箱格式、非空检查），错误提示使用抖动动画+红色边框
- 表单提交模拟：显示加载状态（Spinner动画），成功后显示Toast通知（右上角滑入，3秒后自动消失），失败显示错误提示
- 集成**动态联系信息**：邮箱地址使用mailto链接，社交媒体图标（GitHub、LinkedIn、Twitter）带有悬停颜色变化
- 可选：集成**简易聊天机器人窗口**（右下角悬浮按钮），点击展开对话框，支持预设的3-5个常见问题自动回复

## 7. 导航与全局组件
- **固定顶部导航栏**：滚动时背景从透明变为毛玻璃效果（backdrop-filter: blur(10px) + 半透明背景），高度从80px收缩至60px，过渡动画200ms
- 导航栏包含：Logo（左侧）、页面链接（中间/右侧）、主题切换按钮
- 移动端（&lt;768px）实现**汉堡菜单**：三条横线变换为X的动画，菜单从右侧滑入，覆盖全屏，链接带 staggered（交错）入场动画
- **自定义滚动条**：窄宽度（6px），圆角，颜色随主题变化
- **回到顶部按钮**：滚动超过500px时出现在右下角，点击后平滑滚动到顶部，带有进度环（显示当前滚动百分比，SVG stroke-dashoffset实现）

## 8. 性能与工程化要求
- **图片懒加载**：所有图片使用 Intersection Observer 实现懒加载，加载前显示骨架屏（Shimmer效果）
- **代码分割**：按路由懒加载各页面的非关键CSS和JavaScript（模拟实现，通过动态import或script标签按需加载）
- **减少重排重绘**：动画元素使用 transform 和 opacity，避免触发layout的属性动画
- **防抖与节流**：滚动事件、窗口resize事件必须节流（throttle）处理，搜索输入框防抖（debounce）300ms

## 9. 无障碍访问（a11y）
- 所有图片必须包含描述性alt文本
- 表单字段必须正确关联label（或使用aria-label）
- 所有交互元素必须可通过键盘访问（Tab导航），焦点状态必须有可见的outline或box-shadow
- 颜色对比度符合WCAG AA标准（4.5:1以上）
- 使用语义化HTML标签（header, nav, main, section, article, footer, button等）
- 为动态内容更新添加ARIA live regions（如Toast通知、表单错误提示）

## 10. 额外加分项（展示高级能力）
- **WebGL背景**：在首页或某个区块实现Three.js或纯WebGL的3D元素（如旋转的地球、流动的曲面）
- **PWA支持**：提供Web App Manifest和Service Worker，支持离线访问（至少缓存静态资源）
- **多语言支持**：实现简易国际化（i18n），支持中英文切换，使用JSON文件管理翻译内容
- **微交互细节**：按钮悬停时的磁吸效果（Magnetic Button）、链接下划线从左到右展开动画、自定义光标（桌面端替换默认光标为圆点，悬停可点击元素时放大）

## 11. 技术约束
- **纯前端实现**：仅使用HTML5、CSS3（可配合Tailwind CSS或原生CSS）、原生JavaScript（ES6+）。**不允许使用React、Vue、Angular等前端框架**，以考察基础DOM操作能力
- 允许使用轻量级库：Three.js（仅用于WebGL部分）、GSAP（动画辅助，可选但建议原生实现）
- 代码必须模块化：使用ES6 Modules组织代码，按功能分离文件（router.js, animations.js, theme.js, modal.js等）
- 最终输出为一个完整的、可运行的HTML文件（或文件结构说明+主入口文件），包含所有内联或嵌入的CSS/JS

## 12. 输出要求
- 提供完整的、可直接运行的HTML代码（单文件优先，多文件需说明结构）
- 代码必须包含详细注释，解释关键设计决策和技术实现
- 在代码顶部或文档中说明实现的功能清单和已完成的评估项
- 确保在Chrome、Firefox、Safari最新版本的兼容性